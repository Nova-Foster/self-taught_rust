/* cargo new    -> make full setup for rust inc. main.rs
 * cargo build  -> Build and make output at ~/target/debug/<name?>
 * cargo run    -> Build and run src
 * cargo check  -> Check it runs BUT no .exe
 */

fn main() {
    println!("Hello, world!");
}
